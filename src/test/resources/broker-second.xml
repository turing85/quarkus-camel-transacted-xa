<configuration xmlns="urn:activemq" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="urn:activemq /schema/artemis-configuration.xsd">
    <core xmlns="urn:activemq:core">
        <paging-directory>./target/artemis-second/default/paging</paging-directory>
        <bindings-directory>./target/artemis-second/default/bindings</bindings-directory>
        <journal-directory>./target/artemis-second/default/journal</journal-directory>
        <large-messages-directory>./target/artemis-second/default/large-messages</large-messages-directory>

        <connectors>
            <connector name="activemq">tcp://localhost:0</connector>
        </connectors>
        <acceptors>
            <acceptor name="activemq">tcp://localhost:0</acceptor>
        </acceptors>

        <max-disk-usage>-1</max-disk-usage>
        <security-enabled>false</security-enabled>
    </core>
</configuration>
